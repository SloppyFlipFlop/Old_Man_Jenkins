console.log(input.length)

// Qustion One
const rowtValue = 128
const colVaule = 8

const finalRowValue;
const finalColVaule;

const rowRange = [0, 128]
const colRange = [0, 8]

let newArray = input.forEach((spot) => {
    const spotArray = spot.split("")
    for(let i = 0; i < spotArray.length; i++){
        if(spotArray[i] === 'F'){
            // make it so that it updated the row range to the new range
            
        }
        
    }
})