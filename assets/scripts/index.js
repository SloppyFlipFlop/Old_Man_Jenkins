console.log(input.length)

// Qustion One
const rowtValue = 128
const colVaule = 8

const finalRowValue;
const finalColVaule;

const rowRange = []

let newArray = input.forEach((spot) => {
    const spotArray = spot.split("")
    for(let i = 0; i < spotArray.length; i++){
        if(spotArray[i] === 'F'){
            finalRowValue = rowValue / 2
            
        }
    }
})